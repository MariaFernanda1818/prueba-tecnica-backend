spring:
  webflux:
    base-path: /api/v1
  r2dbc:
    url: r2dbc:postgresql://137.184.136.186:5432/bd_principal?schema=prueba_tecnica
    username: admin
    password: 16042004Bb#$19

  application:
    name: backend

server:
  port: 8001

springdoc:
  swagger-ui:
    path: /swagger-ui
    url: /api/v1/v3/api-docs
    config-url: /api/v1/v3/api-docs/swagger-config
  api-docs:
    path: /v3/api-docs
  default-produces-media-type: application/json
  default-consumes-media-type: application/json
  packages-to-scan: co.tu.paquete.controller

  # 👇 Aquí fuerzas que los esquemas sean HTTPS
  pre-configured-schemes:
    - https

